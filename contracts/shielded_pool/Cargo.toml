[package]
name = "shielded-pool"
version = "0.1.0"
edition = "2021"
authors = ["Lichen Team"]
description = "Fail-closed compatibility ABI for Lichen's historical shielded pool"

[workspace]

[dependencies]
lichen-sdk = { package = "lichen-contract-sdk", path = "../../sdk" }

[lib]
crate-type = ["cdylib", "rlib"]
