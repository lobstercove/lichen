# Session Handover - Mainnet Readiness Audit - 2026-04-25

## Current Resume Packet

- Last refreshed: 2026-04-26 05:06:15 +0400.
- Restart from: final response.
- Durable progress files: this handover plus `MAINNET_READINESS_EXECUTION_TRACKER_2026-04-25.md`.
- Most recent completed slice: final mainnet-ready exit-criteria ledger.
- Part 10 result: active public claim scan found remaining "instant finality" claims only in marketplace pages; those are now changed to "fast BFT commitment" / "Fast BFT commitment" in `marketplace/index.html`, `marketplace/browse.html`, `marketplace/create.html`, `marketplace/item.html`, and `marketplace/profile.html`. Local ignored internal docs `DEPLOYMENT_STATUS.md` and `docs/strategy/BLOCKCHAIN_PUBLIC_CLAIM_CORRECTIONS.md` were also annotated to classify older entries as historical/provenance context rather than automatically-current TODOs; those ignored docs were not force-added to the public commit.
- Part 10 validations passed: targeted active public scan for `instant finality`, `32-slot`, `mainnet ready`, `mainnet-ready`, `production ready`, `Production Ready`, `v0.5.7`, `v0.5.8`, `v0.5.6`, `0.4.8`, and `not wired` returned no actionable matches after the marketplace patch; `npm run test-frontend-assets` passed with 244 asset checks and 2 shared-helper checks; `git diff --check` passed.
- Part 10 durable-state update: `memories/repo/current-state.md` now records that active public/developer surfaces have been cleaned for the audited stale claim strings while older deployment/audit/changelog/strategy docs remain historical unless rechecked.
- Final decision ledger: `MAINNET_READINESS_EXECUTION_TRACKER_2026-04-25.md` now records a scoped mainnet-candidate decision with conditions: include the new/untracked release artifacts in the commit, let CI/release gates run, and reconcile live deployment state before an actual mainnet launch.
- Next immediate action: final response to the user summarizing the durable checkpoint and remaining release conditions.
- Part 8 manual local-stack result: `scripts/qa/e2e-rpc-coverage.js` has been hardened so transport/protocol failures no longer count as passes, invalid JSON is a failure, and REST probes use the actual `/api/v1/*` route prefixes. A dead-port check against `RPC_URL=http://127.0.0.1:1` now fails as intended with `RPC Coverage: 0 passed, 129 failed, 0 skipped`. The first trapped live-stack rerun failed honestly with `RPC Coverage: 128 passed, 1 failed, 0 skipped` because the candle REST probe used `interval=1m` while the server expects numeric seconds; the probe is now `interval=60`. The final trapped local-stack command passed: direct `node scripts/qa/e2e-rpc-coverage.js` reported `129 passed, 0 failed, 0 skipped`; `bash scripts/qa/test-rpc-comprehensive.sh` repeated the same coverage successfully; `bash scripts/qa/test-cli-comprehensive.sh` reported `PASSED: 28`, `FAILED: 0`, `SKIPPED: 0`.
- Part 9 dependency/provenance slice result: `.gitignore` no longer ignores npm lockfiles, so root `package-lock.json` and `sdk/js/package-lock.json` are now visible for tracking. `.github/workflows/ci.yml` has a new JS/Python dependency-health job using `npm ci --ignore-scripts`, `npm audit --omit=dev`, and Python SDK install plus `pip check`; existing CI npm installs now use lockfiles. `.github/workflows/release.yml` now grants OIDC/attestation permissions, attests each release archive and `SHA256SUMS` with `actions/attest@v4`, and includes a GitHub CLI attestation verification command in the draft release notes.
- Part 9 final result: dependency checks, release provenance, OpenSSF Scorecard, active README supply-chain/install guidance, and Docker/compose packaging are aligned with current binary names and RPC health methods. Added `.github/workflows/scorecard.yml`; updated `README.md`; updated `infra/Dockerfile.lichen`, `infra/Dockerfile.custody`, `infra/Dockerfile.market-maker`, and `infra/docker-compose.yml`; updated `memories/repo/current-state.md`.
- Part 9 validations passed: `cargo deny check --config deny.toml advisories licenses sources`; all-lockfile `cargo audit -q -D warnings --file <lockfile>` loop over 34 Cargo.lock files; `npm ci --ignore-scripts && npm audit --omit=dev`; `cd sdk/js && npm ci --ignore-scripts && npm audit --omit=dev && npm run build`; isolated Python venv `pip install -e ./sdk/python` plus `pip check`; `npm run test-wallet-extension && npm run test-wallet`; `cargo check -p lichen-validator -p lichen-custody`; `GRAFANA_PASSWORD=dummy docker compose -f docker-compose.yml config`; `GRAFANA_PASSWORD=dummy docker compose -f infra/docker-compose.yml config`; Ruby YAML parse for all four workflows; stale Docker string scan for `method":"health`, `custody-bridge`, `lichen-rpc`, and `rust:1.75`; `git diff --check`. Local global `python3 -m pip check` is not a repo failure: Homebrew Python 3.14 has `wheel` without `packaging`; the isolated venv check passed.
- Part 8 result so far: frontend static baseline passed and `npm run audit-frontend-rpc-parity` now reports 0 unknown live RPC calls. The previous 7 unknown calls were removed by disabling unsupported signed shielded submission flows in wallet/extension, keeping shielded note state local, and changing program verification submission to a local queued result instead of probing a missing RPC endpoint. Developer portal CLI/RPC/getting-started examples now use active release `v0.5.9` / `lichen 0.5.9`, and the RPC reference now describes the current 182 JSON-RPC dispatch-name surface without retaining the stale 72-REST-endpoint count. SDK/WebSocket/website examples now consistently present public testnet first and say to switch to mainnet endpoints only when targeting mainnet. The programs portal now distinguishes 28 genesis contracts plus the standalone MT-20 template and avoids unsupported uptime/production-ready overclaims.
- Part 8 tracked edits: `wallet/js/shielded.js`, `wallet/extension/src/pages/full.js`, `wallet/extension/src/popup/popup.js`, `programs/js/lichen-sdk.js`, `developers/rpc-reference.html`, `developers/cli-reference.html`, `developers/getting-started.html`, `developers/ws-reference.html`, `developers/sdk-js.html`, `developers/sdk-rust.html`, `website/index.html`, `programs/index.html`, `programs/js/playground-complete.js`, `scripts/qa/e2e-rpc-coverage.js`, `scripts/qa/test_wallet_audit.js`, and `scripts/qa/test_wallet_extension_audit.js`.
- Part 8 static closure validations passed: `npm run test-frontend-assets` (244 asset checks + 2 shared-helper checks); `npm run test-wallet` (91 checks); `npm run test-wallet-extension` (86 checks); `npm run audit-frontend-rpc-parity` (0 unknown live RPC calls, 25 dynamic/manual calls listed); `node --check monitoring/js/monitoring.js`; `node --check wallet/js/wallet.js`; `node --check dex/dex.js`; `node --check explorer/js/explorer.js`; focused `node --check` on touched wallet/extension/program files including `programs/js/playground-complete.js`; stale developer-page scan for `v0.5.7` / `lichen 0.5.7` / `0.5.7`; endpoint/claim scan for stale "switch for mainnet", old WebSocket labels, `29 Production-Ready`, `99.9% uptime`, and `Production Ready`; `git diff --check`.
- Part 7 result: CLI/Rust SDK/JS SDK/Python SDK baselines are green; Python live-validator scripts are now skipped by default unless `LICHEN_RUN_LIVE_SDK_TESTS=1`; SDK/API docs and developer portal samples now match current package names, public testnet examples, RPC version `0.5.9`, and ML-DSA dependency names.
- Part 7 tracked edits: `developers/rpc-reference.html`, `developers/sdk-python.html`, `sdk/python/lichen/connection.py`, `sdk/python/deep_stress_test.py`, `sdk/python/test_sdk_live.py`, `sdk/python/test_websocket_sdk.py`, and `sdk/python/test_websocket_simple.py`.
- Part 7 ignored-doc local edits: `docs/api/RUST_SDK.md`, `docs/api/PYTHON_SDK.md`, `docs/api/JAVASCRIPT_SDK.md`, and `docs/guides/RPC_API_REFERENCE.md` are under ignored `docs/` but were patched in the workspace and recorded here/tracker.
- Part 7 validations passed: `cargo check -p lichen-cli --tests`; `cargo test -p lichen-cli -- --nocapture`; `cd sdk/rust && cargo check --tests && cargo test -- --nocapture`; `cd sdk/js && npm run test && npm run build && node test_cross_sdk_compat.js`; `cd sdk/python && ./venv/bin/python -m pytest -q` with 108 passed / 3 skipped; stale-string `rg` scan for the fixed doc mismatches; `git diff --check`.
- Next immediate action: continue Part 8 with broader frontend endpoint/network selector, dead-link, stale package-name, and honest-disabled-state review.
- Most recent completed slice: `lichenbridge` explicit transfer-status handling, retry-preserving confirmations, checked nonce/locked/validator accounting, null-safe entrypoint reads, and deadline-overflow expiry handling.
- `dex_amm` result: token/native sends now use checked `send_tokens`; second-leg add-liquidity failure refunds token A; failed swap output attempts to refund input; collect-fees and protocol-fee paths no longer silently clear unpaid amounts; remove-liquidity fails before accounting changes when no payout succeeded and records unpaid second-token payouts after partial delivery.
- SDK test harness addition: `sdk/src/lib.rs` and `sdk/src/crosscall.rs` now support queued mock cross-call responses so partial transfer-success/failure regressions can be tested.
- Earlier `dex_amm` validations passed: `cd contracts/dex_amm && cargo test --release`; `cd sdk && cargo test crosscall::tests --release`; `cd contracts/dex_amm && cargo fmt -- --check`; `rustfmt --edition 2021 --check --config skip_children=true sdk/src/lib.rs sdk/src/crosscall.rs`; `cargo fmt --all -- --check`; `git diff --check`.
- `dex_router` result: direct, split, registered multi-hop, and explicit multi-hop swaps now treat failed/unconfigured/zero-output cross-contract legs as route execution failure code `7` instead of recording successful zero-output swaps when `min_out=0`; split routes now pass proportional minimums to both legs and use checked output addition.
- `dex_router` validations passed: baseline `cd contracts/dex_router && cargo test --release` before the patch; after patch `cd contracts/dex_router && cargo test --release`; `cd contracts/dex_router && cargo fmt -- --check`; `cargo fmt --all -- --check`; `git diff --check`.
- `dex_margin` result: zero-size positions are rejected; required margin multiplication now uses `u128`; open-interest decrements on close/liquidation/partial-close now use entry notional instead of current mark notional.
- `dex_margin` validations passed: focused `open_position_zero`, `open_interest`, and `required_margin_uses_u128` release tests; full `cd contracts/dex_margin && cargo test --release` suite with 113 unit tests and 28 adversarial tests; `cd contracts/dex_margin && cargo fmt -- --check`; `cargo fmt --all -- --check`; `git diff --check`.
- `dex_rewards` result: reward claims now treat `Ok(false)` token/native transfer status as failure and preserve pending balances; trade/LP reward math uses `u128`; zero trade records are rejected before stats mutation; checked counters avoid release-mode wraps.
- `dex_rewards` validations passed: baseline and final `cd contracts/dex_rewards && cargo test --release` (final 59 tests); focused `false_transfer_status`, `u128`, and `rejects` release filters; `cd contracts/dex_rewards && cargo fmt -- --check`; `cargo fmt --all -- --check`; `git diff --check`.
- `dex_governance` result: governance execution now has a one-time DEX core dependency setter wired in genesis; new-pair execution uses the quote token that was proposed/voted on; downstream nonzero return statuses keep proposals retryable; fee-change proposals require reputation and fee bounds; DEX core now rejects maker rebates below `-MAX_FEE_BPS`.
- `dex_governance` validations passed: baseline and final `cd contracts/dex_governance && cargo test --release` (final 44 tests); focused `core_address`, `propose_fee_change`, and `execute_new_pair` release filters; `cd contracts/dex_core && cargo test update_fees --release`; final `cd contracts/dex_core && cargo test --release`; `cargo test -p lichen-genesis --release`; governance/core/root fmt checks; `git diff --check`.
- `dex_analytics` result: trade/PnL ingestion now requires the configured authorized caller instead of letting arbitrary traders spoof analytics; trade/candle/trader counters now saturate instead of wrapping; PnL updates return overflow before storing invalid biased values.
- `dex_analytics` validations passed: baseline and final `cd contracts/dex_analytics && cargo test --release` (final 29 tests); focused `rejects_direct`, `saturates`, and `record_pnl` release filters; `cd contracts/dex_analytics && cargo fmt -- --check`; `cargo fmt --all -- --check`; `git diff --check`; `python3 scripts/qa/update-expected-contracts.py --check`.
- `lichenswap` result: outbound transfers now require explicit `Ok(true)` success, reputation discounts are capped at 10,000 bps, reputation bonus and flash-loan limit math use wider/checked arithmetic, and flash-loan repayment/reserve accounting now fails closed on reserve-plus-fee overflow.
- `lichenswap` residual note: the patch intentionally stayed scoped to transfer-status decoding, bounded discounts, and flash/protocol arithmetic guards; it did not perform a broad legacy AMM accounting rewrite because the SDK `Pool` helper mutates storage before some outbound transfers.
- `lichenswap` validations passed: baseline and final `cd contracts/lichenswap && cargo test --release` (final 34 tests); focused `flash_loan` and `reputation_discount` release filters; `cd contracts/lichenswap && cargo fmt -- --check`; `cargo fmt --all -- --check`; `git diff --check`.
- `thalllend` result: configured oracle failures/zero prices now fail closed for collateral-sensitive actions; transfer outs require explicit `Ok(true)` status; deposit, utilization, collateral limit, borrow-index, interest, flash-repay, and counters use checked/u128/saturating arithmetic; flash-loan borrow/repay now use the reentrancy guard.
- `thalllend` validations passed: baseline `cd contracts/thalllend && cargo test --release` before patch (52 tests); focused `overflow`, `oracle`, `transfer_status`, `utilization`, and `liquidation_limit` release filters; final `cd contracts/thalllend && cargo test --release` (61 tests); `cd contracts/thalllend && cargo fmt -- --check`; `cargo fmt --all -- --check`; `git diff --check`.
- `prediction_market` result: lUSD payouts now require explicit `Ok(true)` transfer status; complete-set redemption now transfers before mutating positions/pools/collateral; collateral-cap additions are checked before escrow; analytics/index counters and hot accounting updates saturate instead of wrapping.
- `prediction_market` validations passed: baseline `cd contracts/prediction_market && cargo test --release` before patch (232 tests across unit/adversarial/core/resolution); focused `transfer_status`, `failed_transfer_preserves_state`, `collateral_overflow`, and `analytics_counters` release filters; final `cd contracts/prediction_market && cargo test --release` (236 tests total); `cd contracts/prediction_market && cargo fmt -- --check`; `cargo fmt --all -- --check`; `git diff --check`.
- `sporepump` result: outgoing LICN payouts now require explicit `Ok(true)` transfer status; token creation records the actual fixed creation fee instead of the caller-supplied parameter; token-counter, cooldown, fee, raised-amount, balance, market-cap, and bonding-curve math now fail closed or saturate instead of wrapping/truncating.
- `sporepump` validations passed: baseline `cd contracts/sporepump && cargo test --release` before patch (47 tests); focused `false`, `create_token`, `cooldown_overflow`, and `bonding_curve_math` release filters; final `cd contracts/sporepump && cargo test --release` (53 tests); `cd contracts/sporepump && cargo fmt -- --check`; `cargo fmt --all -- --check`; `git diff --check`; `python3 scripts/qa/update-expected-contracts.py --check`.
- `lichenid` result: reserved-name admin registration now requires the real caller to be the stored admin; premium-name outbid refunds must return explicit `Ok(true)` before auction state changes; `Ok(false)` refund status fails closed; identity/name/attestation/achievement/contribution counters and name expiry math no longer wrap; registration/vouch cooldown deadline checks use saturating addition; given-vouch indexing is separated from received-vouch count.
- `lichenid` RPC/core companion result: RPC now reads the contract's 16-byte FNV skill-attestation count keys with legacy fallback and uses `vouch_given_count` for direct given-vouch scans; processor-side LichenID achievement/contribution counter increments saturate.
- `lichenid` validations passed: baseline `cd contracts/lichenid && cargo test --release` before patch (65 tests); focused `vouch_uses_separate_given_count`, `register_identity_counter_overflow`, `register_cooldown_overflow`, `false_refund_status`, `admin_register_reserved_name`, `register_name_expiry_overflow`, `finalize_name_auction_rejects_caller_mismatch`, `refund_requires_token_config`, and `refund_requires_self_address` release filters; final `cd contracts/lichenid && cargo test --release` (71 tests); three focused LichenID RPC handler release tests; `cargo check -p lichen-rpc --tests`; `cargo test -p lobstercove-lichen-core achievement --release` compile/filter gate; contract and root fmt checks; `git diff --check`; `python3 scripts/qa/update-expected-contracts.py --check`.
- `lichendao` result: treasury transfers are now bound to the executed proposal's target contract and approved `treasury_transfer\0 + token + recipient + amount` action hash; proposal counters/end-times/vote/veto totals now fail closed on overflow; proposals record/refund the actual configured stake threshold; cancellation only marks cancelled after a successful stake refund; failed post-execution stake refunds are recorded for retry through `claim_proposal_stake_refund`; unsafe pointer entrypoints in the reviewed surface reject null pointers.
- `lichendao` validations passed: baseline `cd contracts/lichendao && cargo test --release` before patch (13 tests); final `cd contracts/lichendao && cargo test --release` (19 tests); `cd contracts/lichendao && cargo fmt -- --check`; `cargo fmt --all -- --check`; `git diff --check`; `python3 scripts/qa/update-expected-contracts.py --check`.
- `lichenoracle` result: asset/query/attestation inputs are bounded and null-safe; zero prices and excessive decimals are rejected before storage/log display math; feed/query/attestation stats now update with saturating counters; duplicate pending randomness commits are rejected; attestation data must hash to the submitted data hash and signature counts fail closed instead of wrapping; reporter keys now use the full 32-byte address instead of the first two bytes.
- `lichenoracle` validations passed: baseline `cd contracts/lichenoracle && cargo test --release` before patch (27 tests); final `cd contracts/lichenoracle && cargo test --release` (33 tests); `cd contracts/lichenoracle && cargo fmt -- --check`; `cargo fmt --all -- --check`; `git diff --check`; `python3 scripts/qa/update-expected-contracts.py --check`.
- `lichenmarket` result: direct offer acceptance now verifies seller ownership, escrows full payment before NFT transfer, refunds on NFT-transfer failure, releases the reentrancy guard on missing offers, rejects expired offer records, and records unpaid seller/royalty payouts instead of silently losing them; auction bidding now fails closed if the previous highest bidder cannot be refunded; zero-price listings are rejected; auction end/anti-sniping and sale/list counters use checked or saturating arithmetic.
- `lichenmarket` validations passed: baseline `cd contracts/lichenmarket && cargo test --release` before patch (28 tests); final `cd contracts/lichenmarket && cargo test --release` (34 tests); `cd contracts/lichenmarket && cargo fmt -- --check`; `cargo fmt --all -- --check`; `git diff --check`; `python3 scripts/qa/update-expected-contracts.py --check`.
- `lichenauction` result: replacement bids escrow before previous-bidder refunds; finalization moves the NFT before seller/royalty payout; offer acceptance escrows full payment before NFT transfer; zero bid/offer, expiry, bid-increment, anti-snipe, pointer, and payout-accounting guards were added.
- `lichenauction` validations passed: baseline `cd contracts/lichenauction && cargo test --release` before patch (34 tests); final `cd contracts/lichenauction && cargo test --release` (41 tests); `cd contracts/lichenauction && cargo fmt -- --check`; `cargo fmt --all -- --check`; `git diff --check`; `python3 scripts/qa/update-expected-contracts.py --check`.
- `lichenpunks` result: mint now requires initialization, metadata/base URI reads are bounded, `transfer_from` verifies the signer and respects pause state, `approve`/`burn` are pause-gated, metadata alias lookup uses the SDK key, and royalty/supply/counter guards were added.
- `lichenpunks` validations passed: baseline `cd contracts/lichenpunks && cargo test --release` before patch (21 tests); final `cd contracts/lichenpunks && cargo test --release` (28 tests); `cd contracts/lichenpunks && cargo fmt -- --check`; `cargo fmt --all -- --check`; `git diff --check`; `python3 scripts/qa/update-expected-contracts.py --check`.
- `bountyboard` result: missing reward-token config now pays/refunds native escrow, malformed token config fails closed, payout/refund failures leave bounties open, bounty/stat counters are guarded, platform fees are capped, and admin/pointer/submission reads are stricter.
- `bountyboard` validations passed: baseline `cd contracts/bountyboard && cargo test --release` before patch (24 tests); final `cd contracts/bountyboard && cargo test --release` (30 tests); `cd contracts/bountyboard && cargo fmt -- --check`; `cargo fmt --all -- --check`; `git diff --check`; `python3 scripts/qa/update-expected-contracts.py --check`.
- `compute_market` result: escrow collection now treats `Ok(false)` as failure and checks job-count overflow before charging; inactive providers cannot claim jobs; cancel/release failures restore job and escrow state for retry; split dispute resolution no longer double-pays after partial payout failure and records unpaid provider payouts; platform fees are capped; provider/completed/dispute counters saturate.
- `compute_market` validations passed: baseline `cd contracts/compute_market && cargo test --release` before patch (40 tests); final `cd contracts/compute_market && cargo test --release` (49 tests); `cd contracts/compute_market && cargo fmt -- --check`; `cargo fmt --all -- --check`; `git diff --check`; `python3 scripts/qa/update-expected-contracts.py --check`.
- `moss_storage` result: LICN transfers now require explicit `Ok(true)`; store-data cost, expiry, and `data_count` overflow fail closed before state writes; provider confirmations validate entry/provider bytes and use checked capacity plus saturating stored counters; rewards preserve vesting on failed or false transfer status; challenge counters saturate; challenge responses reject null/oversized payloads; slash math uses `u128` and failed redistribution payouts are recorded as unpaid.
- `moss_storage` validations passed: baseline `cd contracts/moss_storage && cargo test --release` before patch (27 tests); final `cd contracts/moss_storage && cargo test --release` (36 tests); `cd contracts/moss_storage && cargo fmt -- --check`; `cargo fmt --all -- --check`; `git diff --check`; `python3 scripts/qa/update-expected-contracts.py --check`.
- `sporepay` result: stream creation now treats `Ok(false)` escrow as failure and checks stream-count overflow before escrow; withdrawals require explicit successful payout and roll back withdrawn amounts on failure; cancel counts saturate; partial cancel settlement records unpaid recipient payouts if sender refund already succeeded and recipient transfer fails, preventing double sender refunds on retry.
- `sporepay` validations passed: baseline `cd contracts/sporepay && cargo test --release` before patch (36 tests); final `cd contracts/sporepay && cargo test --release` (43 tests); `cd contracts/sporepay && cargo fmt -- --check`; `cargo fmt --all -- --check`; `git diff --check`; `python3 scripts/qa/update-expected-contracts.py --check`.
- `sporevault` result: unsafe 32-byte entrypoint reads now reject null pointers through a shared helper; LICN outbound transfers require explicit `Ok(true)`; deposit verifies signer before fees and defers fee/share/asset state writes until checked accounting passes; failed first deposits do not accrue fees; withdrawals restore exact protocol-fee/share/asset state when transfer fails; protocol-fee withdrawals preserve fees on false transfer status; strategy allocation math is checked; harvest deployed/yield/performance-fee math uses wide or saturating arithmetic.
- `sporevault` validations passed: baseline `cd contracts/sporevault && cargo test --release` before patch (51 tests); final `cd contracts/sporevault && cargo test --release` (59 tests); `cd contracts/sporevault && cargo fmt -- --check`; `cargo fmt --all -- --check`; `git diff --check`; `python3 scripts/qa/update-expected-contracts.py --check`.
- `shielded_pool` result: shield/unshield/transfer now reject zero values, duplicate commitments/nullifiers, oversized proof/encrypted-note/request payloads, null/oversized ABI buffers, and corrupt stored state; shield pool-balance overflow and transfer commitment-cap failures are preflighted before mutation; WASM state saves now fail closed instead of returning success after serialization failure.
- `shielded_pool` validations passed: baseline `cd contracts/shielded_pool && cargo test --release` before patch (10 tests); final `cd contracts/shielded_pool && cargo test --release` (23 tests); `cd contracts/shielded_pool && cargo fmt -- --check`; `cargo fmt --all -- --check`; `git diff --check`; `python3 scripts/qa/update-expected-contracts.py --check`.
- `lichenbridge` result: token/native payouts now require explicit `Ok(true)`; failed/false mint and unlock transfers no longer consume the confirming validator's retry; lock amount, nonce, validator count, and expired-unlock reserve restoration use checked arithmetic; bridge deadline overflow is treated as expired; source/destination chain zero hashes are rejected; raw 32-byte entrypoint reads now reject null pointers.
- `lichenbridge` validations passed: baseline `cd contracts/lichenbridge && cargo test --release` before patch (53 tests); final `cd contracts/lichenbridge && cargo test --release` (60 tests); `cd contracts/lichenbridge && cargo fmt -- --check`; `cargo fmt --all -- --check`; `git diff --check`; `python3 scripts/qa/update-expected-contracts.py --check`.
- Part 5 contract/genesis catalog audit is complete through all 29 in-tree contract directories and 28 genesis contracts.
- Immediate action: start Part 7 SDKs/CLI/developer portal contract review.
- Compaction-safety rule: after each contract-family slice, update this `Current Resume Packet` first, then append command/finding evidence to the tracker.

Primary durable files:

- `MAINNET_READINESS_FULL_PASS_PLAN_2026-04-25.md`
- `MAINNET_READINESS_EXECUTION_TRACKER_2026-04-25.md`

Standing user instruction:

- Before executing each task, submit the question, provisional answer, and action.

Current worktree scope:

- `sdk/js/test_cross_sdk_compat.js`: JS SDK golden-vector test fixed for ESM and duplicate helper removal.
- `package.json`: added `audit-frontend-rpc-parity`.
- `scripts/qa/audit_frontend_rpc_parity.js`: new frontend/RPC parity checker.
- `core/src/block.rs`: block structure validation now uses actual bincode serialized size and has a PQ-heavy regression test.
- `core/src/transaction.rs`: transaction validation now caps PQ signatures, caps actual serialized transaction size, and rejects oversized wire payloads before JSON/bincode deserialization.
- `core/src/mempool.rs`: mempool admission now validates transaction structure before hashing or sender lookup.
- `rpc/src/lib.rs`: RPC transaction decoding/body limits now align with the core transaction cap, and central submission validates before enqueueing.
- `validator/src/wal.rs`: consensus WAL now stores signed local prevote/precommit records until checkpoint.
- `validator/src/consensus.rs`: startup restore hooks repopulate signed-vote suppression state and verify recovered vote signatures; late precommits after `RoundStep::Commit` no longer emit duplicate `CommitBlock` actions.
- `validator/src/main.rs`: local prevotes/precommits are written to WAL before broadcast, and broadcast is skipped if durable persistence fails.
- `validator/src/main.rs`: BFT commit, sync, and pending-block state-root mismatches now fail closed before storing/applying mismatched blocks.
- `validator/src/main.rs`: live blocks deferred to BFT no longer count as sync progress; actual BFT commits record progress after checkpointing.
- Checkpoint root/finality review produced no code changes: current code verifies checkpoint contents, finalized slot, and committed block authenticity, including post-effects checkpoint roots distinct from block header roots.
- `p2p/src/network.rs`: P2P now validates block/transaction/compact-block admission before gossip rebroadcast or queue admission, with logical caps for compact-block tx IDs, `GetBlockTxs`, and `BlockTxs`.
- `faucet-service/src/http_support.rs`: trusted proxy client-IP extraction now parses and normalizes forwarded IP headers before using them as faucet rate-limit keys.
- `custody/src/deposit_api_support.rs`: deposit auth replay lookup now happens before charging deposit rate-limit quota.
- `custody/src/withdrawal_api_support.rs`: withdrawal auth replay lookup now happens before charging withdrawal rate-limit quota.
- `custody/src/tests/deposit_sweep_rebalance.rs` and `custody/src/tests/policy_and_creation.rs`: idempotent replay tests now retry without clearing rate-limit state and assert quota is only charged once.
- `custody/src/withdrawal_broadcast_support.rs`: PQ approval checks now enforce the per-job effective withdrawal signer threshold.
- `custody/src/withdrawal_broadcast_support/evm.rs`: EVM Safe assembly now requires and packs the per-job effective withdrawal signer threshold.
- `custody/src/tests/signing_and_assets.rs`: added regression coverage for job-required EVM Safe thresholds above the static config threshold.
- `custody/src/withdrawal_settlement_support.rs`: withdrawal worker now stops before broadcast if `record_tx_intent` fails.
- `custody/src/sweep_execution_support.rs`: sweep worker now stops before broadcast if `record_tx_intent` fails.
- `custody/src/credit_execution_support.rs`: credit worker now stops before submit if `record_tx_intent` fails.
- `custody/src/tests/withdrawal_and_misc.rs`: added a missing-`tx_intents` regression for withdrawal pre-broadcast fail-closed behavior.
- `custody/src/chain_confirmation_support.rs`: Solana confirmations now reject non-null `err`; EVM confirmations now require receipt status `0x1` / `1`.
- `custody/src/withdrawal_settlement_support.rs`: terminal external confirmation errors now mark broadcasting withdrawals `permanently_failed`.
- `custody/src/tests/withdrawal_and_misc.rs`: added reverted EVM receipt regression for outbound withdrawals.
- `custody/src/reserve_ledger_support.rs`: added `adjust_reserve_balance_once` with per-movement markers for idempotent stablecoin reserve accounting.
- `custody/src/sweep_execution_support.rs`: sweep reserve increment now happens through the idempotent movement marker before final confirmed status.
- `custody/src/withdrawal_settlement_support.rs`: stablecoin withdrawal reserve debit now happens through the idempotent movement marker before final confirmed status.
- `custody/src/credit_execution_support/builder.rs`: sweep credit job IDs are deterministic (`credit:<sweep_job_id>`) to avoid duplicate queued credits after replay.
- `custody/src/tests/deposit_sweep_rebalance.rs`: added reserve movement deduplication coverage.
- `scripts/clean-slate-redeploy.sh`: now requires exact `LICHEN_CLEAN_SLATE_REDEPLOY_CONFIRM` before any destructive VPS redeploy step and selects custody DB/service/port, faucet behavior, and Cloudflare endpoint per network.
- `scripts/vps-post-genesis.sh`: now uses `custody-db-mainnet` and `lichen-custody-mainnet` for mainnet and skips faucet key/restart behavior outside testnet.
- `scripts/qa/test_local_helper_guards.sh`: now covers the clean-slate redeploy confirmation guard.
- `docs/deployment/PRODUCTION_DEPLOYMENT.md`: local ignored runbook copy updated with the clean-slate confirmation phrase and mainnet note.
- `p2p/src/network.rs`: expensive request throttling is centralized and now covers `CheckpointMetaRequest` as well as status/snapshot/state-snapshot/FindNode requests.
- `core/src/keypair_file.rs`: added regression coverage proving legacy plaintext keypair files require explicit compatibility opt-in.
- `rpc/src/lib.rs`: native, Solana-compatible, EVM-compatible, and REST rate-tier classifiers now cover proof generation, bridge deposit creation, scan-heavy reads, external bridge lookups, full block reads, and shielded commitment/proof reads more conservatively; Solana `getSignatureStatuses` now rejects batches over 256 signatures.
- `rpc/src/lib.rs`: added native JSON-RPC `getMarketplaceConfig`, backed by `FeeConfig` NFT mint/collection fees and marketplace fee bps storage/defaults.
- `rpc/src/ws.rs`: WebSocket global/per-IP connection slots are now reserved before accepting the upgrade, released on failed upgrade, and released on socket cleanup.
- `rpc/tests/rpc_full_coverage.rs`: added an oversized Solana `getSignatureStatuses` regression test.
- `rpc/tests/rpc_full_coverage.rs`: added coverage for `getMarketplaceConfig`.
- `scripts/qa/update-expected-contracts.py`: now checks contract directories against `GENESIS_CONTRACT_CATALOG` and treats `mt20_token` as the only known in-tree non-genesis contract.
- `developers/contract-reference.html`: documents that `mt20_token` is an in-tree MT20 template/compatibility contract and is intentionally not deployed at genesis.
- `sdk/src/token.rs`: shared MT-20 helper now rejects zero/invalid token-standard inputs, rejects self-transfer before balance writes, and preserves allowance when `transfer_from` fails.
- `contracts/mt20_token/src/lib.rs`: standalone MT-20 wrapper now rejects zero owner initialization and covers self-transfer/failed-transfer allowance regressions.
- `contracts/dex_core/src/lib.rs`: `place_order` now runs post-only and reduce-only rejection/capping checks before token escrow, and reduce-only escrow is calculated from the capped position size.
- `contracts/dex_amm/src/lib.rs`: AMM transfers now use checked token/native sends in tests and production; liquidity, swap, fee, protocol-fee, and remove-liquidity paths retain or record unpaid accounting on transfer failure instead of silently clearing or burning balances.
- `sdk/src/lib.rs` and `sdk/src/crosscall.rs`: test mock cross-calls now support queued responses for partial success/failure regression coverage.
- `contracts/dex_router/src/lib.rs`: routed swaps now fail on failed/zero-output cross-contract legs instead of recording phantom zero-output successes; split routes enforce proportional per-leg minimums.
- `contracts/dex_margin/src/lib.rs`: position opening and lifecycle accounting now reject zero size, avoid required-margin overflow, and decrement open interest by entry notional rather than mark notional.
- `contracts/dex_rewards/src/lib.rs`: reward claims now require successful transfer status and reward accounting uses checked/u128 math for trade, referral, and LP accrual paths.
- `contracts/dex_governance/src/lib.rs`, `contracts/dex_governance/abi.json`, and `genesis/src/lib.rs`: DEX governance now stores voted quote tokens, has `set_core_address` opcode 20, and genesis wires DEX core for proposal execution.
- `contracts/dex_core/src/lib.rs`: direct fee updates now reject maker rebates below `-MAX_FEE_BPS` and maker rebate math uses `unsigned_abs`.
- `contracts/dex_analytics/src/lib.rs`: analytics ingestion now fails closed without the authorized DEX caller and uses saturating/checked accounting for trade, candle, trader, and PnL stats.
- `contracts/lichenswap/src/lib.rs`: transfer-status, reputation-discount, and flash-loan arithmetic guards added with focused regressions.
- `contracts/thalllend/src/lib.rs`: configured oracle fail-closed handling, explicit transfer success checks, u128 lending arithmetic, checked flash repayment, saturating counters, and flash-loan reentrancy guards added with focused regressions.
- `contracts/prediction_market/src/lib.rs`: lUSD transfer-status, complete-set redemption payout ordering, collateral-cap checks, and saturating analytics/accounting guards added with focused regressions.
- `contracts/sporepump/src/lib.rs`: LICN transfer-status, exact creation-fee accounting, token-counter/cooldown guards, and bonding-curve overflow-safe math added with focused regressions.
- `contracts/lichenid/src/lib.rs`: reserved-name admin signer binding, vouch-given counter separation, auction refund fail-closed state ordering, cooldown/counter/expiry arithmetic hardening, and focused regressions.
- `rpc/src/lib.rs` and `rpc/tests/rpc_handlers.rs`: LichenID attestation count lookups now match the contract's 16-byte FNV key with legacy fallback, and direct given-vouch scans use `vouch_given_count`.
- `core/src/processor/achievement_detection.rs`: LichenID achievement and contribution counter increments now saturate instead of wrapping.
- `contracts/lichendao/src/lib.rs`: treasury transfers now require the proposal target to be this DAO and the transfer args to hash to the approved treasury action; proposal/vote/veto arithmetic fails closed; actual stake thresholds are stored/refunded; failed execution refunds are retryable; cancellation refund failure preserves proposal state.
- `contracts/lichenoracle/src/lib.rs`: bounded oracle asset/query/data reads, zero-price/decimal rejection, duplicate pending randomness commit rejection, attestation hash verification and count overflow guards, full-address reporter keys, and focused regressions.
- `contracts/lichenmarket/src/lib.rs`: offer acceptance now uses payment escrow before NFT transfer, expired offers are rejected, previous-bid refund failure preserves auction state, zero-price listings are rejected, end-time/counter arithmetic is guarded, and unpaid payout accounting records failed seller/royalty releases.

Completed validations:

- `node --check sdk/js/test_cross_sdk_compat.js`
- `node sdk/js/test_cross_sdk_compat.js`
- `cd sdk/js && npm run test`
- `node --check scripts/qa/audit_frontend_rpc_parity.js`
- `python3 scripts/qa/update-expected-contracts.py --check`
- `cd contracts/lichenid && cargo test vouch_uses_separate_given_count --release`
- `cd contracts/lichenid && cargo test register_identity_counter_overflow --release`
- `cd contracts/lichenid && cargo test register_cooldown_overflow --release`
- `cd contracts/lichenid && cargo test false_refund_status --release`
- `cd contracts/lichenid && cargo test admin_register_reserved_name --release`
- `cd contracts/lichenid && cargo test register_name_expiry_overflow --release`
- `cd contracts/lichenid && cargo test finalize_name_auction_rejects_caller_mismatch --release`
- `cd contracts/lichenid && cargo test refund_requires_token_config --release`
- `cd contracts/lichenid && cargo test refund_requires_self_address --release`
- `cd contracts/lichenid && cargo test --release`
- `cargo test -p lichen-rpc test_get_lichenid_skills_with_attestations --release`
- `cargo test -p lichen-rpc test_get_lichenid_vouches_bidirectional --release`
- `cargo test -p lichen-rpc test_get_lichenid_profile_and_directory --release`
- `cargo check -p lichen-rpc --tests`
- `cargo test -p lobstercove-lichen-core achievement --release`
- `cd contracts/lichenid && cargo fmt -- --check`
- `cd contracts/lichendao && cargo test --release`
- `cd contracts/lichendao && cargo fmt -- --check`
- `cd contracts/lichenoracle && cargo test --release`
- `cd contracts/lichenoracle && cargo fmt -- --check`
- `cd contracts/lichenmarket && cargo test --release`
- `cd contracts/lichenmarket && cargo fmt -- --check`
- `cargo fmt --all -- --check`
- `git diff --check`
- `python3 scripts/qa/update-expected-contracts.py --check`
- `npm run test-frontend-assets`
- first-party JS/MJS `node --check` sweep
- `npm run audit-frontend-rpc-parity` expected failure with 8 unknown live frontend RPC methods
- `cargo fmt --all -- --check`
- `cargo test -p lobstercove-lichen-core block::tests::test_validate_structure --release`
- `cargo test -p lobstercove-lichen-core transaction::tests::test --release`
- `cargo test -p lobstercove-lichen-core mempool::tests::test_mempool --release`
- `cargo test -p lichen-rpc test_l01_rejects --release`
- `cargo test -p lichen-validator wal::tests::test_signed_vote --release`
- `cargo test -p lichen-validator consensus::tests::test_restore_signed --release`
- `cargo test -p lichen-validator consensus::tests::test_precommit_after_commit_does_not_emit_duplicate_commit --release`
- `cargo test -p lichen-validator consensus::tests::test_precommit --release`
- `cargo test -p lichen-validator consensus::tests::test_commit_block_includes_commit_signatures --release`
- `cargo test -p lichen-validator latest_verified_checkpoint --release`
- `cargo test -p lichen-validator verify_checkpoint_anchor --release`
- `cargo test -p lichen-p2p p2p_admission --release`
- `cargo test -p lichen-p2p network::tests --release`
- `cargo test -p lichen-validator verify_checkpoint_anchor --release`
- `cargo test -p lichen-faucet http_support --release`
- `cargo test -p lichen-faucet --release -- --nocapture`
- `node faucet/faucet.test.js`
- `cargo test -p lichen-custody reuses_existing --release`
- `cargo test -p lichen-custody policy_and_creation --release`
- `cargo test -p lichen-custody deposit_sweep_rebalance --release`
- `cargo test -p lichen-custody signing_and_assets --release`
- `cargo test -p lichen-custody test_process_signing_withdrawals_requires_tx_intent_before_broadcast --release`
- `cargo test -p lichen-custody test_process_broadcasting_withdrawals_marks_reverted_evm_tx_failed --release`
- `cargo test -p lichen-custody test_reserve_ledger_adjust_once_deduplicates_movement --release`
- `cargo test -p lichen-custody test_process_sweep_jobs_confirmed_enqueues_credit_and_updates_status --release`
- `cargo test -p lichen-custody --release`
- `cargo check -p lichen-custody -p lichen-faucet --tests`
- `cargo test -p lichen-faucet --release -- --nocapture`
- `node faucet/faucet.test.js`
- `git diff --check`
- `bash -n scripts/clean-slate-redeploy.sh`
- `bash -n scripts/vps-post-genesis.sh`
- `bash -n scripts/qa/test_local_helper_guards.sh`
- `bash scripts/qa/test_local_helper_guards.sh`
- `cargo test -p lichen-p2p test_expensive_request_classification_includes_checkpoint_meta --release`
- `cargo test -p lichen-p2p network::tests --release`
- `cd contracts/lichenswap && cargo test flash_loan --release`
- `cd contracts/lichenswap && cargo test reputation_discount --release`
- `cd contracts/lichenswap && cargo test --release`
- `cd contracts/lichenswap && cargo fmt -- --check`
- `cargo fmt --all -- --check`
- `git diff --check`
- `cd contracts/thalllend && cargo test overflow --release`
- `cd contracts/thalllend && cargo test oracle --release`
- `cd contracts/thalllend && cargo test transfer_status --release`
- `cd contracts/thalllend && cargo test utilization --release`
- `cd contracts/thalllend && cargo test liquidation_limit --release`
- `cd contracts/thalllend && cargo test --release`
- `cd contracts/thalllend && cargo fmt -- --check`
- `cargo fmt --all -- --check`
- `git diff --check`
- `cd contracts/prediction_market && cargo test transfer_status --release`
- `cd contracts/prediction_market && cargo test failed_transfer_preserves_state --release`
- `cd contracts/prediction_market && cargo test collateral_overflow --release`
- `cd contracts/prediction_market && cargo test analytics_counters --release`
- `cd contracts/prediction_market && cargo test --release`
- `cd contracts/prediction_market && cargo fmt -- --check`
- `cargo fmt --all -- --check`
- `git diff --check`
- `cd contracts/sporepump && cargo test false --release`
- `cd contracts/sporepump && cargo test create_token --release`
- `cd contracts/sporepump && cargo test cooldown_overflow --release`
- `cd contracts/sporepump && cargo test bonding_curve_math --release`
- `cd contracts/sporepump && cargo test --release`
- `cd contracts/sporepump && cargo fmt -- --check`
- `cargo fmt --all -- --check`
- `git diff --check`
- `python3 scripts/qa/update-expected-contracts.py --check`
- `cargo test -p lichen-validator load_startup_genesis_config --release`
- `cargo test -p lichen-validator updater::tests --release`
- `cargo test -p lobstercove-lichen-core test_plaintext_keypair_load_requires_explicit_compat --release`
- `cargo check -p lichen-validator -p lichen-p2p --tests`
- `git diff --check`
- `cargo test -p lichen-rpc test_m02 --release`
- `cargo test -p lichen-rpc test_solana_get_signature_statuses_rejects_oversized_batch --release`
- `cargo check -p lichen-rpc --tests`
- `cargo fmt --all -- --check`
- `git diff --check`
- `cargo test -p lichen-rpc ws_connection_reservation_enforces_per_ip_limit_and_releases --release`
- `cargo check -p lichen-rpc --tests`
- `cargo fmt --all -- --check`
- `git diff --check`
- `cargo test -p lichen-rpc --test rpc_full_coverage --release -- --nocapture`
- `cargo test -p lichen-rpc --test shielded_handlers --release -- --nocapture`
- `python3 scripts/qa/update-expected-contracts.py --check`
- `python3 -m py_compile scripts/qa/update-expected-contracts.py`
- `git diff --check`
- `cd sdk && cargo test token::tests --release`
- `cd contracts/mt20_token && cargo test --release`
- `cd contracts/lusd_token && cargo test --release`
- `cd contracts/weth_token && cargo test --release`
- `cd contracts/wsol_token && cargo test --release`
- `cd contracts/wbnb_token && cargo test --release`
- `rustfmt --edition 2021 --check sdk/src/token.rs`
- `cd contracts/mt20_token && cargo fmt -- --check`
- `cd contracts/dex_core && cargo test reduce_only --release`
- `cd contracts/dex_core && cargo test post_only --release`
- `cd contracts/dex_core && cargo test --release`
- `cd contracts/dex_core && cargo fmt -- --check`
- `cargo test -p lichen-rpc test_native_get_marketplace_config --release`
- `npm run audit-frontend-rpc-parity` expected-failed with 7 unknown call sites across 6 remaining methods after `getMarketplaceConfig` was resolved.
- `cargo check -p lichen-rpc --tests`
- `cargo fmt --all -- --check`
- `git diff --check`

Critical finding fixed:

- `P2-01`: signed BFT prevotes/precommits are now durably persisted before broadcast and restored on startup so the validator cannot sign a conflicting vote for the same height/round/type after restart.
- `P2-02`: late or replayed same-height precommits after the engine entered `RoundStep::Commit` no longer emit duplicate `CommitBlock` actions.
- `P2-03`: malformed P2P block/transaction/compact-block payloads are now rejected before gossip rebroadcast or queue admission.
- `P2-04`: non-default state-root mismatches in BFT commit and sync/full-validation paths now fail closed before block storage/effects.
- `P2-05`: live blocks deferred to BFT no longer falsely complete sync progress; progress is recorded when BFT actually commits.
- `P6-01`: faucet trusted-proxy headers no longer allow arbitrary malformed strings to become per-IP rate-limit keys; forwarded addresses must parse as real IPs.
- `P6-02`: custody deposit/withdrawal retries with identical signed auth now return existing records before rate-limit cooldowns are charged; new requests still consume quota.
- `P6-03`: custody broadcast and EVM Safe assembly now enforce the per-job effective signer threshold, so elevated/extraordinary withdrawals cannot be finalized with only the lower static config threshold.
- `P6-04`: custody withdrawal, sweep, and credit workers now fail closed before broadcast/submission if the write-ahead tx intent cannot be recorded.
- `P6-05`: custody withdrawal confirmations now treat Solana `err` and EVM reverted receipts as terminal failures instead of confirmations or ordinary pending states.
- `P6-06`: custody stablecoin reserve movements are now per-job idempotent, and sweep credit job IDs are deterministic to avoid duplicate queued credits after replay.
- `P3-01`: automated clean-slate VPS redeploy now fails closed without an exact confirmation phrase, and its mainnet path no longer reuses testnet custody/faucet service assumptions.
- `P3-02`: P2P checkpoint metadata requests now share the per-peer expensive-request rate limit instead of bypassing snapshot/status/DHT throttles.
- `P3-03`: startup genesis selection and updater release policy were reviewed as current-code false positives, and plaintext keypair load policy now has focused regression coverage for explicit compatibility opt-in.
- `P4-01`: public RPC rate-tier coverage now treats STARK proof generation and bridge deposit creation as expensive, treats scan/full-object/external-lookup methods as moderate across native/Solana/EVM/REST surfaces, and caps Solana `getSignatureStatuses` batches at 256 entries.
- `P4-02`: marketplace mint-fee frontend parity is fixed with `getMarketplaceConfig`. Remaining parity gaps are not safe RPC aliases: shielded submit calls pass proof fragments rather than signed transactions, `sendShieldedTransaction` carries wallet password material, `getShieldedNotes` lacks viewing-key semantics, and `submitProgramVerification` has no server queue.
- `P4-03`: WebSocket connection limits are now reserved before upgrade, closing the handshake race where concurrent upgrades could exceed global or per-IP limits before `handle_socket` incremented counters.
- `P4-04`: CORS/error-response review required no code change. Mainnet wildcard CORS is fail-closed, origins are exact-host matched, storage/path errors are sanitized, and legacy admin RPC tests cover public disablement plus local/dev bearer and loopback requirements.
- `P5-01`: contract inventory checks now enforce that every genesis catalog entry has a directory and that only `mt20_token` may exist in-tree outside genesis; the public contract reference documents that non-genesis status.
- `P5-02`: shared MT-20 self-transfer could inflate balances and failed `transfer_from` calls could consume allowance before the transfer succeeded. The SDK token helper now rejects invalid zero/self-transfer inputs and only decrements allowance after a successful transfer; the standalone `mt20_token` wrapper rejects zero owner initialization and has focused regressions.
- `P5-03`: `dex_core::place_order` could escrow funds before post-only and reduce-only rejection paths, and reduce-only capping happened after escrow. Rejection/capping now happens before escrow, and the reduce-only escrow field uses the capped order size.
- Checkpoint-root concern was verified as a false positive on current code; focused tests passed.

Next step:

- Continue Part 5 core DeFi review with `dex_amm`, then `dex_router`, `dex_margin`, `dex_rewards`, `dex_governance`, and `dex_analytics`.

Resume note after compaction failure:

- Restart point confirmed from the tracker: Part 5.1 is complete; continue with the token/wrapped-asset contract family.
- Preserve progress in this file and `MAINNET_READINESS_EXECUTION_TRACKER_2026-04-25.md` after each contract-family slice before moving on.
- Token/wrapped-asset slice is now complete; resume from the core DeFi contract family.
- DEX Core sub-slice is now fixed/validated; resume Task 5.3 at `dex_amm`.

Validation caveat:

- `cd sdk && cargo fmt -- --check` currently fails because untouched `sdk/src/dex.rs` and `sdk/src/nft.rs` have pre-existing rustfmt drift. Focused `rustfmt --edition 2021 --check sdk/src/token.rs`, root `cargo fmt --all -- --check`, MT-20 contract fmt, and `git diff --check` passed.

Resume note after `lichenauction` slice:

- Checkpoint time: 2026-04-26 00:04:12 +0400.
- `contracts/lichenauction/src/lib.rs` is fixed and validated.
- Baseline `cd contracts/lichenauction && cargo test --release` passed before the patch: 34 tests.
- Final `cd contracts/lichenauction && cargo test --release` passed: 41 tests.
- Safety changes: null-safe address reads; checked auction/offer expiry, bid-increment, and anti-snipe math; zero bid/offer rejection; saturating counters; unpaid-payout records for post-NFT payout failures; escrow-required checks instead of zero-address fallback.
- Transfer-order changes: replacement bids escrow before previous-bidder refunds, finalization moves the NFT before seller/royalty payout, and offer acceptance escrows full payment before NFT transfer with refund/payout recording on NFT failure.
- Extra validation passed: `cd contracts/lichenauction && cargo fmt -- --check`, `cargo fmt --all -- --check`, `git diff --check`, and `python3 scripts/qa/update-expected-contracts.py --check`.
- Resume Part 5 NFT/marketplace at `contracts/lichenpunks`.

Resume note after `lichenpunks` slice:

- Checkpoint time: 2026-04-26 00:08:50 +0400.
- `contracts/lichenpunks/src/lib.rs` is fixed and validated.
- Baseline `cd contracts/lichenpunks && cargo test --release` passed before the patch: 21 tests.
- Final `cd contracts/lichenpunks && cargo test --release` passed: 28 tests.
- Safety changes: null-safe address/byte reads; initialization gating before mint; bounded metadata/base URI reads; zero recipient rejection; signer verification and pause gating for `transfer_from`; pause gating for `approve` and `burn`; saturating transfer/burn counters; recipient-balance overflow prechecks.
- Collection metadata/admin changes: `get_punk_metadata()` now reads the SDK `metadata:` key, `set_royalty()` caps at 1000 bps, and `set_max_supply()` rejects positive caps below current supply.
- Extra validation passed: `cd contracts/lichenpunks && cargo fmt -- --check`, `cargo fmt --all -- --check`, `git diff --check`, and `python3 scripts/qa/update-expected-contracts.py --check`.
- NFT/marketplace family is complete. Resume Part 5 at agent/storage/payments: `bountyboard`, `compute_market`, `moss_storage`, `sporepay`, and `sporevault`.

Resume note after `bountyboard` slice:

- Checkpoint time: 2026-04-26 00:12:21 +0400.
- `contracts/bountyboard/src/lib.rs` is fixed and validated.
- Baseline `cd contracts/bountyboard && cargo test --release` passed before the patch: 24 tests.
- Final `cd contracts/bountyboard && cargo test --release` passed: 30 tests.
- Safety changes: null-safe address reads; checked `bounty_count` increment; saturating completed/cancel/stat counters; strict admin-state checks; platform-fee cap at 1000 bps; submission-length validation before payout.
- Escrow changes: missing reward-token config now pays/refunds native escrow through the zero-address system token, malformed token config fails closed, and payout/refund failures leave bounties open.
- Extra validation passed: `cd contracts/bountyboard && cargo fmt -- --check`, `cargo fmt --all -- --check`, `git diff --check`, and `python3 scripts/qa/update-expected-contracts.py --check`.
- Resume Part 5 agent/storage/payments at `contracts/compute_market`, then `moss_storage`, `sporepay`, and `sporevault`.
