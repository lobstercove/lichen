# v0.5.271 Production-Readiness Addendum

Date: 2026-09-02

Final status: failed closed in immutable release run `33673803471`, Archive V2
job `100393542870`, during the strict four-validator DEX journey. No deployable
release was produced and `v0.5.271` was not installed on Testnet. Superseded by
the narrow `v0.5.272` candidate; the compatibility findings below remain valid.

## Why a new release is required

The complete `v0.5.270` hosted release matrix passed and produced signed,
provenance-attested artifacts. Its coordinated live deployment stopped before
repair or restart because the repair helper used the wrong state-path flag.
The corrected read-only command then exposed a historical deployed contract
ABI encoded with `name` where the current decoder accepted only `contract`.
The Singapore stop also showed that blocking `systemctl stop` could hide the
existing bounded kill path while a legacy read-only FUSE request was in kernel
wait. No chain data or validator identity was reset or copied.

The full timeline, hashes, preserved state, and escaped-test analysis are in
`docs/deployment/V0_5_270_TESTNET_DEPLOYMENT_INCIDENT_2026-09-02.md`.

## Narrow corrections

- `ContractAbi.name` accepts legacy `name` as a Serde alias while retaining
  canonical `contract` serialization.
- The guarded Testnet repair regression stores the exact legacy ABI shape and
  proves that code/ABI replacement preserves contract owner and storage.
- The coordinated deploy helper passes `--db-path` and binds its destructive
  confirmation to `repair-dex-contracts:testnet:v0.5.271`.
- Service stop uses nonblocking systemd control, reads actual PIDs from
  `cgroup.procs`, performs a bounded control-group kill, and fails closed if a
  process remains.
- Independent release jobs run concurrently; checksum generation is still
  blocked on quality/security, Archive V2 parity, contract, compiler, and every
  platform build.

## Evidence reused without code drift

The DEX Core/CLOB, concentrated-liquidity AMM, direction-aware router,
governance/listing, launchpad/graduation, rewards, prediction-market,
WebSocket, consensus, outage/rejoin, and Archive V2 implementations are
unchanged from `v0.5.270`. Its clean and hosted four-validator gate passed
volume/prediction 144/144, launchpad/governance 104/104, fresh role joins,
one-validator outage, own-state and all-validator restarts, and exact logical
public-history manifest parity. Those results remain relevant evidence; the
new release gates additionally cover the compatibility and deployment delta.

## Focused qualification

- `cargo check -p lichen-validator`: passed.
- Legacy ABI decode and canonical serialization regression: passed.
- Production-shaped stopped-state DEX repair preservation regression: passed;
  1/1 targeted test with 499 unrelated validator tests filtered out.
- Archive/release static QA: 106/106 passed.
- Rolling release custody and service sequencing QA: passed.
- Public claims, frontend integrity, exchange readiness, shell syntax,
  ShellCheck, Actionlint, lockfile metadata under `--locked`, and diff checks:
  passed.

The immutable tag workflow remains authoritative for the full quality,
security, contract, compiler, four-validator Archive V2, platform build,
checksum, signature, and provenance result.

## Live acceptance and cleanup boundary

All four validators must install the same signed artifact and repair their own
preserved database while stopped. No validator starts unless all four repairs
are idempotent and their contract/storage evidence matches. Acceptance then
requires advancing finality, four-voter convergence and authorship, RPC and
WebSocket health, DEX/AMM/CLOB/prediction/governance/launchpad smokes, and the
same genesis-to-tip Archive V2 logical manifest.

Legacy/FUSE rows and files are reclaimed only through signed, source-backed,
range-bound retirement with measured before/after bytes. The canonical US
source tail remains until four-way parity is sealed. R2 object deletion remains
outside this release and requires a separately approved exact-key manifest.
