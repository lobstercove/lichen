# Checkpoint history compatibility for receiving Archive V2 roles

Status: successor under qualification. Production remains signed v0.5.281 with
Archive V2 disabled. No v0.5.283 artifact is signed, staged or deployed.

The immutable v0.5.282 release run34050267481 failed archive job101532503863
at2026-09-06T18:46:32Z. Fresh verified-cache V3 applied checkpoint10000 with
history9101..10000, then failed admission because its configured5000-slot
hot window required5001..10000. The harness had already verified checkpoint20000.
Failed job log SHA256:
`cb2e151d2d143c9988383edb05008fb06034339ba2d49be178a148e929e54834`.

The complete local v282 matrix passed separately:147 exchange and104 launchpad
checks, all fresh roles, source outage, own/all restarts, and four identical
21-category public-history manifests at30000. Root:
`4f84a5367a1d6bd460e9a884433219fadf7e8ab8d050b88cf787c7a83ad26c40`.
Local proof85f961a02a2479d6439dfe2a63af92704531731e7a70d41393b4ab6ba6d833b9
does not override the hosted failure.

Discovery validated the advertised profile's minimum retention and authenticated
catalog handoff, but omitted the receiving role's configured hot window. In
local development the general minimum is deliberately one slot, so the valid
900-slot profile reached state replacement before role admission rejected it.
Public networks also need the configured check when retention exceeds the
50000-slot general minimum. This defect predates the v282 reader-memory fix.

The receiver now retains its validated role configuration for checkpoint
discovery and checks the profile before admitting metadata into its candidate
set. It repeats the check before live replacement. Verified-cache and consensus
roles require history starting at or before their configured hot-window start.
Full-archive recovery retains its catalog-backed admission path. Existing root,
certificate, history completeness, predecessor binding, replay-overlap and
admission checks remain mandatory. No consensus, wire, state or archive format
changes are introduced.

Regression cases cover the observed900/5000 mismatch, exact and one-slot-short
boundaries, the compatible20000 checkpoint, both bounded roles, full archive,
ordinary recovery,100000-slot public retention, genesis and integer extremes.
Both focused test methods passed in the release profile on the successor
implementation. Complete workspace/CI/release matrices and sustained fleet
memory acceptance remain pending until their dated results are recorded.
