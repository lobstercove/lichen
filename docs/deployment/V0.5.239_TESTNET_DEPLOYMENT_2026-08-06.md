# v0.5.239 Testnet Deployment — 2026-08-06

## Outcome

Signed release `v0.5.239` at
`84ec5fb4907f70d3c5916eb226955fda5e7acd35` is installed and running on all
four `lichen-testnet-1` validators. The fleet is finalizing with all four
validator identities participating in BFT. Validator state, WAL, keys, cold
history, access configuration, Archive V2 objects, and signed rollback
artifacts were preserved.

Archive V2 retirement is **not** complete. Runtime Archive V2 remains disabled
on all four validators, and no legacy row has been retired. The first two V2
segments and their catalog remain verified evidence for the next bounded
maintenance window.

## Release evidence

- tag/commit: `v0.5.239` /
  `84ec5fb4907f70d3c5916eb226955fda5e7acd35`
- release workflow: `31050017649`, all jobs passed
- Linux x86-64 archive SHA-256:
  `566aa8c0c3f20e0c8365164e5c900f1ee06c563982824783d2656e9b86145a68`
- detached signature file SHA-256:
  `f6a514d1c466cbc52a69a75d48971de05ffd94e32712205b2f54e65f003fcf52`
- release signer:
  `8HitBNnh8qbhfne5NCv2yHrQFoD6xbmHcWaUSgCGtsk`
- installed/running validator SHA-256 on every host:
  `4efac6ba4356463bec78d45e60c686d53849d1cff7a1ed7e735e37d3426a2959`

The tag workflow, GitHub provenance, release checksums, and detached PQ
signature were verified before promotion. Exact signed v0.5.238 binaries are
retained as the immediate schema-3 rollback artifacts; v0.5.237 remains the
deeper schema-3 rollback anchor.

## Deployment and live acceptance

All four validators were stopped before binary promotion and started from the
same signed artifact. EU then replayed its preserved gap using ordinary
source-backed 100-block range responses and caught up without copied state or
a checkpoint shortcut.

An interrupted Archive V2 retirement pass later held EU's RocksDB lock during
its initial equivalence scan. It was stopped while the retirement journal was
still absent, proving the command had not reached deletion. EU restarted from
its unchanged state. The live three-validator quorum advanced too quickly for
v0.5.239's zero-slot pre-consensus admission gate, leaving EU caught up and
broadcasting votes but not yet inside its BFT proposal loop. SEA and IN were
briefly paused to freeze the tip. EU reached exact parity and logged entry into
BFT at height `10,235,558`; SEA and IN then both re-entered BFT from that same
height before finality resumed.

Final acceptance evidence:

- public slots advanced `10,235,602 -> 10,235,618` in five seconds;
- all four `getValidators` records had vote/activity lag zero (one sample had
  the usual one-slot cross-origin skew);
- the 41-block range `10,235,570..10,235,610` contained proposals from every
  identity: EU 10, SEA 6, IN 12, and US 13;
- EU appeared in all 41 sampled canonical commit certificates;
- fixed slot `10,235,600` matched on all four validators with block hash
  `4d11fec74498a6ae556212f07ff3df6beeaf6471d4bfe70d79bbbeef444fb468`
  and state root
  `34668298659ebe8b63961ecf1ff728f38681da2b5e0be51cdfc0bdaf50f7ae0b`;
- every service was active with zero systemd restarts, a one-second-old head,
  and the exact running v0.5.239 validator hash;
- canonical and Explorer RPC advanced, the Explorer root returned HTTP 200,
  and both public `/edge-health` endpoints returned HTTP 200.

Final observed free bytes were:

| Region | Free bytes | Approx. GiB |
| --- | ---: | ---: |
| US | 6,256,340,992 | 5.83 |
| EU | 8,145,051,648 | 7.59 |
| SEA | 7,937,331,200 | 7.39 |
| IN | 6,515,666,944 | 6.07 |

These values remain emergency headroom, not capacity approval.

## Online-safe cleanup

The signed v0.5.239 staging directory was removed from `/tmp` on every host
after installed/running hash verification. Six obsolete v0.5.238 restage
trees were also removed from US. Package archives were cleaned and archived
system journals were bounded to 64 MiB. The validated staging paths accounted
for about 755 MB apparent bytes on US and 145 MB on each other host; sparse or
shared extents and concurrent chain growth make the net filesystem gain
smaller than those apparent sizes.

No live binary, rollback binary, validator database, cold archive, WAL, key,
identity, V2 catalog/segment/retirement proof, evidence directory, source
devchain, or R2 object was removed.

## Archive V2 state and next gate

EU retains verified local segments `0..49,999` and `50,000..99,999`. Both
objects, manifests, and the schema-3 catalog are verified in the independent
R2 `enam` and `apac` buckets. Segment 0 has a signed retirement authorization:

- object hash:
  `2ed2c026206a545dc5529aa11df658f8c86e8747d57cb2c53f539ade43c7f9a5`
- retirement manifest SHA-256:
  `5ceb444032e1f47e251624f35ee389adad333fcc73272c919a3fd57d391c6dd6`
- catalog semantic root:
  `a5b4ca698c68cf5cda5d13949e6b325be0f618a49401421b6352df1086f7a9f7`

The retirement journal does not exist, no tombstone was applied, and
`archive_v2.enabled` is false on all four live validators. The next retirement
attempt requires an explicit maintenance window. It must let one bounded pass
finish cleanly, preserve the 5 GiB reserve, enable the schema-3 V2 reader before
that validator restarts, prove early-slot RPC fallback, and use a frozen-tip
consensus re-entry procedure if the remaining quorum advanced during the pass.

The full genesis-to-tip retrofit still requires constructing and independently
verifying the remaining segments and cataloging the exact approved historical
loss declaration for `2,872,006..4,298,999`. A release cannot synthesize that
unavailable history.
