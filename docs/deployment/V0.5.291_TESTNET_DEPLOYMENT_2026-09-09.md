# Signed v0.5.291 Testnet deployment — September 9, 2026

Current continuation, September 10 at 08:52 UTC: all four validators have
completed adoption of the verified 462-segment catalog through slot `12,990,000`.
Each passed native admission, four-validator authorship and common finality;
their maintenance guards were cleared and auxiliary services restored. Both R2
sources contain catalog 462. The binary release remains signed v0.5.291 and was
published at 07:42:07 UTC, with all seven asset digests reverified. The exact
legacy R2 cleanup is in progress: 6,000 primary-bucket objects have complete
per-key deletion receipts. A following validator-list RPC gate returned a busy
response before another deletion; direct checks showed the fleet still advancing.
Continuation repeats fresh acceptance and excludes completed objects using the
validated ledger. No pending uncertain request remains. This is an active
operation observation; full cleanup and remaining hot reclaim are not complete.

The final catalog continuation is
`64072ea7c2c5d4be84384164fc2241daa56ec02a4f1b4c706cebb10eee4cda03`.
Full four-host admission/finality receipt:
`a4043e5b030e5dee9eda3e011c1eeaa2fae31298c54530b8eb7be6de10de85af`.
India's completed maintenance receipt:
`6ff32cf1ebed015413991a974c5854c81f2247f13fccf2f9f47b4ed2ffd25369`.
Fresh finality after the first R2 batch:
`56b90a70db97eb2252a05e722b63cc8359c0f94ab284af0d84559a84bdc1496b`.

Catalog 462 has SHA-256
`6e12989274fb378ecfac1237a0035cb6dbc06d6decda194e5d22ca98ead11b33`
and root `f1477cbe18e5629f826e8ba96884cff314a31f3a1b655ec444e64fbacea8bac7`.
Its two-source publication receipt is
`21d2a027af57e579720711a1900ca8fb5ac86e16b89924385cc796987b15683d`.
Seven appended ranges were built from the preserved EU checkpoint at
`13,040,000`; both local object copies passed native whole-object verification.
All prior entries and the exact Testnet waiver were preserved. This publication
adds 70,000 blocks and 82,310 transactions to the preceding 455-segment catalog.
Catalog 455 was published and fully index-audited but was never adopted by the
validators because its restart margin had expired before that operation began.

The 462 transition plan is
`b1850f6587b9048d59de40720d8df4d936a4bad9b12693f53e779060c07e1c0a`.
All 44 operator tests passed locally and as the actual `lichen` service account
on the VPS ext4 data filesystem. Qualification explicitly set and checked
`TMPDIR`: an earlier test harness used a tmpfs default despite residing on ext4.
That first result was retained and was not treated as actual-data-filesystem
qualification. The accepted qualification is
`b9bd2a2e39c088a04c185bd96953f8bf871080287e1a511c0eecea673caa407b`.
The US prewarm receipt is
`abcd049e2da9a83e99ad10ad0fe4c4be0b45d37cd113ccbd3f59ed58430fc7b7`.
Each host must complete its own native restart admission, four-author health
check, auxiliary restoration and halt-guard removal before the next host stops.

The earlier US nine-segment hot retirement failed before creating any journal
or tombstone. An isolated reference recovered 194,070 authentic account index
rows, with native imports and idempotence checks, but the subsequent native
equivalence check found a canonical block absent from both local hot and cold
stores. All nine journals remain absent. Slot and account-index coverage alone
does not establish physical source equivalence for every public-history family.
The failed attempt, source captures and authorization records are preserved;
no check was waived and no missing block or checkpoint metadata was invented.
US recovered from its own WAL/state before this later catalog adoption.

A separate read-only composition check completed against authenticated archive
indexes. It reproduced the exact 17,003 `tx_meta` rows and native digest from the
four-validator tail proof, then checked 6,657,396 prefix keys against those tail
keys with zero overlaps, duplicate signatures or conflicts. It wrote isolated
SQLite scratch and opened no validator database. Receipt:
`cb03bcf60c19dbbb7400efdaa0f516073e26ef65b2dcce27fadda8658c3fae6f`.
The completed review combines the retained full native prefix, unchanged waiver,
source-backed catalog appends, four independent native tails and this key check
through fixed slot `12,970,000`:
`984ede7a38e9470cb2cbd4a9c76e04b3961138a6e05dd76adc797d580b20a070`.
It is a composed proof, not a new full native manifest or deletion authorization.

The next US retirement candidates are catalog entries 183 through 191, covering
`10,249,000..10,338,999`: nine ranges of 10,000 blocks each. Native read-only
checks found all 90,000 block bodies locally. Separate native dry-run checks of
the authenticated index rows found every account-history and transaction-metadata
row physically present and identical, with no missing, conflicting or incomplete
rows. The 18 checks took 46.9 seconds and wrote no live database records:
`faa67c2ab7d67ab097eee6e1794c6826c83051b54cb0ea530d52cd546c17a629`.
All five canonical physical categories subsequently passed for all nine ranges:
45 native checks, each matching its authenticated archive commitment and finding
every expected physical row locally. The final check initially encountered a
live RocksDB log rotation while opening read-only (`229172.log` had disappeared).
The first 44 completed proofs were retained; only the final `tx_to_slot` check was
repeated against the unchanged validator process, and it passed. No validator
was stopped or database written by either check. The combined canonical receipt
is `6303f979c33455f77ddf19165f488fcdd43a5c63b92787e12594fee6522ebbe8`.
Together with the authenticated raw-category checks above, this establishes all
21 physical categories for the selected 90,000 blocks. The native retirement
pass must still run its own equivalence gate before any deletion. These ranges
do not require the account-index backfill used for the earlier failed group.

The selected nine objects were staged and read back from both archive sources;
all nine native retirement authorizations completed without live database writes.
Stage receipt: `48629cff7d8d00738b16ddd56fdb77bf08941205f1f69a944d765005fd8f2cec`.
Authorization: `6d994e57d711f112a46df8d3fa9ea9e683512d8abaad1cf684b4a47d60a23cf3`.
The upcoming compaction cap is 1 GiB of input. Its 31,139,862,938-byte minimum
retains the full 26,777,786,778-byte reserve, twice the input for rewrite space,
2 GiB of growth and 64 MiB for records. The complete operator passed 26 retained
lifecycle tests and 24 scope/failure tests as the actual service user on VPS ext4:
`f5a43802886327081d436457224386cab31f701e6c81a502c98e8069068804f0`.
It rejects stale runtime evidence and an exhausted catalog restart margin before
stopping the validator. No hot journal, tombstone or compaction has been executed
for this group at this checkpoint.

A separate cleanup removed nine obsolete local staging objects after whole-copy
verification of both active replacements and confirmation that the failed older
retirement had created no journals. It unlinked 3,713,826,816 allocated bytes;
observed free space increased from 32,212,422,656 to 35,921,002,496 bytes while
the validator kept running. Active objects, catalogs, authorizations and recovery
captures were preserved. Receipt:
`80339e1d842eb82dad358316a1a7ea0c57e188db1aaf0bbdc0bded3e7b321f38`.

PR 79 has been merged as `02b808c8e5cead9947b4cf8bbad0159f5a293a41`.
Its tree `420cd8041c04ba7f0d352ce7b21acb7b8eaaf211` exactly matches the immutable
signed v0.5.291 source tree. The tag was not moved. GitHub release publication
completed; final hot reclamation and eligible R2 cleanup remain in progress.
The exact R2 cleanup dispatcher has passed 11 additional tests on the actual
local APFS filesystem using all 28 proposed batches. It refreshes fleet health,
retired-path references and object metadata before each batch and retains a
durable intent and per-key outcome. Its accepted plan is
`ab40b9aaca14c49b02856e15dca16d271475577b07c3e9ea0d6709e634ba6bc3`,
covering exactly 26,156 objects and 1,758,366,002,974 bytes across both buckets.
Active archives and every object outside the 100 verified obsolete emergency-SST
prefixes are protected by exact inventory comparison. Progress is recorded above;
the full cleanup has not yet completed.

The dated observations below record the original 452 transition and subsequent
maintenance. Their process bindings and catalog values are historical.

At 18:19 UTC all four `lichen-testnet-1` validators had completed the coordinated
signed v0.5.291 and Archive V2 catalog 452 transition. Native admission, installed
and running binary parity, advancing four-validator authorship, common canonical
finality, transaction-counter deltas and auxiliary-service restoration passed.
The maintenance halt guards were cleared. This is a dated observation; fresh
process and storage checks remain necessary before another operation.

| Binding | Accepted value |
| --- | --- |
| Signed source commit | `b2931a648fb4974f4ed7b103a58e24719bb8a501` |
| Validator SHA-256 | `de8a0c4e7dbf77dedafb38addfb6b62768709f8780477d47b4ef106db7770bd8` |
| Archive CLI SHA-256 | `f0836b2fdf47d53a5744c011eef24747acac8eb0aa4b7f6292d22af99f60bfac` |
| Catalog SHA-256 | `a1388380a6352bb492dd5b7e184f989e5758f2e8e56f1c684bf5cf6844934ca9` |
| Catalog root | `e690707f4c3b829e004df6cdbfa28ecf8ae9de47f5c93f1fa654863f96f9b03f` |
| Catalog coverage | 452 segments, through slot `12,890,000` |
| Fleet | US `15.204.229.189`, EU `37.59.97.61`, SEA `15.235.142.253`, IN `148.113.43.247` |
| Role | `verified_cache`, deep history enabled on all four |

The [CI run](https://github.com/lobstercove/lichen/actions/runs/34347567603)
passed all 15 jobs. The [tag release workflow](https://github.com/lobstercove/lichen/actions/runs/34354314013)
passed all 11 jobs; all five platform artifacts, provenance and detached PQ
checksum signatures were verified before installation. No locally built
candidate replaced a validator binary. GitHub release publication and source
consolidation were still pending at this observation.

The first maintenance admission attempt incorrectly reused initial bootstrap
after legacy cold retirement. That CLI requires local genesis and failed before
startup. The signed runtime already supports this restart through the existing
verified role marker and complete local hot/archive handoff checks. The corrected
operator preserved the failed outputs, own state/WAL and source verification.
Each validator completed native marker-based admission before the paused fleet
was released. No genesis was reconstructed, no state was copied, and no initial
bootstrap success was fabricated. A second operator issue repeated expensive
source checks inside a short process barrier; the continuation retained completed
immutable-source observations and refreshed process checks separately.

The four-way continuation receipt is
`402e016c2381165793d3de76d31b341666ec108073dbb3c6138fa110d517f5cd`;
the completed transition acceptance is
`c66eafba0e2b277eeb889b5148b253da79052f2fa2a0124a4396e4c3cb9ebbf7`.
Every validator reported zero source failures and zero quarantined objects.
The 60-second counter observation counted 28 canonical OracleAttestation
transactions and verified an exact increase from `7,909,426` to `7,909,454` on
every node. No historical metrics repair was replayed.

An actual archived wallet Activity page was then measured with the same public
benchmark account, limit 20 and `before_slot=12562001` used before deployment.
All four returned the same complete page and exact continuation cursor:

| Validator | First request | Repeated request |
| --- | ---: | ---: |
| US | 2.525 s | 2.648 s |
| EU | 5.435 s | 0.197 s |
| SEA | 10.893 s | 3.174 s |
| IN | 4.232 s | 0.931 s |

US and SEA had previously exceeded 30 seconds; IN took 17.31 seconds and EU
5.81 seconds. These observations cover this real 20-row archived page, not every
account or a browser-rendering latency guarantee. Sparse accounts and first
archive reads still need separate performance observation.

The completed native full-history verification used a fixed tip of `12,850,000`
and covered all 21 public-history categories. The independent replica check
verified the full old 443-object set; the nine appended ranges were subsequently
published and read back from both sources. The existing exact Testnet legacy-loss
waiver for signed block bodies `2,872,006..4,298,999` remains unchanged. It applies
to no fresh network or Mainnet.

All four unused legacy cold copies were removed, recovering
`150,709,530,624` bytes of actual filesystem space. By 19:11 UTC, all 100 old
emergency-SST R2 mounts were also retired: US 49, EU 15, Singapore 20 and India 16.
Each used normal unmounts with zero forced or lazy unmounts; validator processes
remained unchanged. Singapore's integrity scan was temporarily held with an
independent restoration timer, then restored with its original unit definitions.
The final mount receipt is
`1508aa0fb4953005dc58bdedfa6d42637bc22387a11604eb67bac56fba9c9b4b`;
integrity restoration is
`6a9143d36acf805d84048747ea385311bf5d4ee29bb5f0051eebf07cdd8b4240`.

No R2 objects had been deleted at that observation. Remaining hot compaction and
eligible obsolete R2 deletion must be recorded before declaring retirement complete.
EU produced a new checkpoint at slot `12,960,000`; the other three still lacked
current periodic checkpoints because their storage planning reserves were not
consistently met. Full verification of one composed archive and two independent
public tails does not establish four-validator public-history parity. That final
parity check and sustained archive publication remain open acceptance items.

At 19:04 UTC the Explorer's configured same-origin `/api/testnet` endpoint
reported advancing blocks and transaction totals `7,910,898` → `7,910,905`, with
TPS approximately 0.47–0.48. A zero/outage was not reproduced by that observation.
Active V2 primary and replica objects, preserved recovery captures, provider
backups and signed rollback sets remain protected. The current 200 GB VPS profile
is not approval for Mainnet or indefinite archive growth.

Follow the [deployment preflight](ARCHIVE_V2_DEPLOYMENT_PREFLIGHT.md) for initial
bootstrap versus restart, actual capacity calculations, explicit systemd property
inspection, exact normal unmounts and separately authorized R2 cleanup.

At 19:58 UTC US completed a bounded maintenance session and rejoined from its
own WAL/state. The new continuation is
`118a96213b3f061bbd8c258f40dca2e7c6584a7d7eacb9534e9bf3a0b2dcc5e4`;
the completed maintenance receipt is
`a2821cff3007fd8f0eb4d3c873e5020582062a65284a5179cb27b068453a712d`.
Its maintenance guard was cleared and auxiliary services restored. Seven small
reclaim calls left two original ranges pending and produced no additional
measured physical gain. Further hot retirement must group sufficient verified
deletions before compacting the shared block/transaction families.

The subsequent complete dependency audit found zero references and zero scan
races across all 100 retired mounts, including after the US restart:
`8bcaacf4df99c5dc853bf9216df655696bc7eb59a57e8baf28ee6fd34213fa64`.
By 20:39 UTC EU, IN and SEA independently matched all 21 public-history
categories over `12,870,001..12,970,000`. The native receipts are
`c5cc47b40a0afc9f74188bc660c24da39b6722c83fa093103f132cc878036a39`
and `9d8f0779082d0b1f14d33e4a2a1e773f0e3b9f56159d48a8429ad8de22cfb504`.
US had skipped its periodic checkpoint because the native planning reserve
exceeded available space. Its separate read-only fixed-tail verification was
still running; these three receipts alone do not prove four-way parity.

At 20:34 UTC the Explorer's actual configured public endpoint again reported
healthy, advancing finality (`12,975,883` → `12,975,983`), Total Transactions
(`7,913,348` → `7,913,365`) and approximately 0.47 TPS. Observation receipt:
`37d85a4dd52895d259e9a8f797d49e8d8214ca5d66cd609eb2e3c92ddc5e9370`.
The latest full fleet health check also confirmed all four authors advancing
at fixed slot `12,976,838`:
`5141eef2666ea26e74dc7cc0baf715d3d3cfd8259758430ca7d9bfe604078964`.
Eligible R2 object deletion and sustained archive publication remain open.

At 21:05 UTC the US read-only native export completed all 21 public-history
categories for `12,870,001..12,970,000`, matching EU, IN and SEA exactly. Its
receipt is `c3e467e361b1153bfb00c6afd9705a16a43621619820958970a3318661e972f7`.
The signed CLI independently confirmed that the active catalog's checkpoint
handoff root matches the three retained checkpoint profiles. The consolidated
four-tail review is
`01f9c29423f6b52676946c06975be40be48ad76db455082b5b96c709f36984fb`.
This proves four matching tails and their common catalog handoff; it does not
represent four new full-history scans or invent a new genesis-to-tip manifest.
The US check used its live database read-only because its periodic checkpoint
had been skipped. No checkpoint metadata was synthesized.

The next US group contains nine verified segments over
`10,024,000..10,248,999`. Whole-object reads from both independently located
R2 buckets and local readback completed before native authorization. All nine
signed retirement records then completed without database writes or validator
stops:
`17089c38eba03ec45635aee4e23ea6a05ced266e77586fce7e152b111cb1f535`.
The initial authorization attempt encountered the already documented missing
`quarantine` staging directory. It produced no signed record; its failed output
was retained and the corrected attempt used a new exclusive output directory.
Directory access and file-descriptor checks alone had not exercised native reader
startup. The actual native authorization completion now verifies that path.

The current group's budget retains `26,777,786,778` bytes for runtime recovery,
twice the `2 GiB` compaction input limit, `2 GiB` state growth and `64 MiB`
additional margin after staging. Native deletion must point-check every public
history category before creating its journal. Complete the group's tombstones
before shared-family compaction, then measure actual allocation and restore the
same validator from its own WAL. These preparations are not reclaimed space.

The public edge health endpoint subsequently reported all four origins ready at
slot `12,984,678`, with lags of 0–4 slots. Total Transactions advanced
`7,914,450` → `7,914,459`, with TPS approximately 0.47–0.53. The reported zero
was not reproduced. The existing edge routes requests across all four origins;
no TLS or origin-auth configuration was changed during these checks.
