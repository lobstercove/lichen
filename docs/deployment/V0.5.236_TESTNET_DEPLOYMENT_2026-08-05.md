# v0.5.236 Testnet Deployment Record — 2026-08-05

## Release identity

- Signed testnet release: `v0.5.236`.
- Tag commit: `749e1cd0cac55c2ca84c2e1ef053b5df5cd1055e`.
- Release workflow: GitHub Actions run `30963027265`, green across release,
  security, contracts, compiler sandbox, exact four-validator hot/cold Archive
  V2 parity, platform builds, provenance, and checksums.
- Linux x86_64 archive SHA-256:
  `697e82dfcda104ed891ff458b164ae61995ec852e5be98d267db8251e3f601cf`.
- Validator SHA-256 installed and running on all four VPSes:
  `f95e9727197705ae9772456450f9acabd9a8ae31d2eef819b7cf01e85c1b0749`.
- `SHA256SUMS` and its detached PQ signature were verified against trusted
  signer `8HitBNnh8qbhfne5NCv2yHrQFoD6xbmHcWaUSgCGtsk`. GitHub artifact provenance
  was verified against the tag commit and release workflow.

The public release is <https://github.com/lobstercove/lichen/releases/tag/v0.5.236>.

## Deployment

The four validators were coordinated-stopped before installation. Official
tag-workflow artifacts were staged, hash-verified, atomically promoted, and
started together. No locally built binary was installed. Every host preserves
the v0.5.229 and v0.5.228 signed rollback executables; validator state, cold
history, consensus WAL, keys, identities, access configuration, and provider
backup storage were preserved.

The repository release verifier completed successfully on all four hosts.
Final acceptance reported `lichen-validator 0.5.236`, the exact installed and
`/proc/<pid>/exe` hash above, `status=ok`, and `NRestarts=0` everywhere.

The final operating topology is:

- US: validator, faucet, and custody active; local health endpoints return 200.
- EU: validator active; faucet and custody intentionally disabled. Legacy cold
  maintenance has a one-hour startup delay and 300-second interval so the node
  can remain in consensus after its capacity-bound catch-up.
- SEA: validator and faucet active; custody intentionally disabled.
- IN: validator and faucet active; custody intentionally disabled.

EU initially remained in one-block catch-up behind a three-validator quorum.
US and SEA were briefly stopped, leaving IN as the sync source without quorum;
EU and IN then matched the same tip hash. US and SEA were restarted together.
EU subsequently reported fresh activity/voting at slot `10,172,351`, within one
slot of the other validators.

## Live acceptance

All four origins returned the same fixed block at slot `10,172,348`:

- block hash: `aa6b869f7376fcdd4f96c1af63c9623d47072c3b64f880222e4520ac246e0cda`
- state root: `0caa3af8720d8d3029dee14b47ec910940fd0c868111b02074df92eea04a86f8`
- commit validator count: 3

The canonical RPC advanced from slot `10,172,169` to `10,172,182` in ten
seconds. `getValidators` returned four validators, Explorer same-origin RPC and
the Explorer root passed, and metrics retained nonzero totals (5,923,873
transactions, 87 accounts, and 32 contracts). Canonical WebSocket
`subscribeSlots` returned both an acknowledgement and a live slot notification.
The DEX oracle returned fresh, non-stale native-consensus feeds, and the pair-2
candle endpoint returned four candles.

Final observed free bytes remained above the signed 5 GiB testnet runtime
floor on every host: approximately 5.64 GB US, 5.99 GB EU, 6.29 GB SEA, and
5.57 GB IN. EU required 388 MB of safe cleanup from apt metadata/cache,
rotated journals, old logs, and unused build tooling before it could cross the
floor. No chain or operator data was deleted. Bounded legacy cold migration is
active or consensus-pressure-paused without a migration error; the runtime
floor was not lowered.

## Public-history manifest evidence

The stopped fixed-tip full public-history scan was started over all 21
categories at slot `10,170,477`. EU and IN completed independently and matched
exactly:

- manifest root: `271949078ded8fdc9eb6bcefd3fa7279c418bbd8ecf64aa8b34c98002717d397`
- canonical blocks: 8,743,484
- transactions: 5,814,836

This is valid two-way genesis-to-tip evidence, not a completed four-way gate.
A separate concurrent operator/session started validators while US and SEA
scanners were still running. SEA then failed while iterating `account_txs`
because primary RocksDB compaction removed SST `154067.sst` beneath the
read-only scanner; US was also invalidated and aborted. The incident produced
no manifest mismatch, but the four-way full-manifest hard gate remains open and
must be repeated with exclusive fleet ownership and secondary/checkpoint-safe
scanner semantics. Do not represent this run as four-way manifest parity.

## Archive V2 status

The signed `lichen-archive-v2` executable is deployed on every VPS (SHA-256
`277b631983ba95748e105d335a3dd9c335d8492668baf820af1ae020b658c55c`),
but live Archive V2 remains `enabled=false`. No legacy history was retired and
no retroactive Archive V2 build was started: the 200 GB roots lack safe staging
and replica capacity. Retroactive conversion remains gated on additional
writable storage or authenticated object storage plus a completed parity gate.

## Cleanup and follow-up

Local `cargo clean` removed 6,232 files (2.7 GiB). A separate active
`codex/archive-v2-retrofit-v237` worktree subsequently rebuilt targets and has
uncommitted source changes; those files were intentionally not modified,
staged, committed, or cleaned by this deployment. The small parity evidence
bundle was retained.

Required follow-up:

1. Provision additional monitored archive/staging capacity.
2. Fix the stopped full-manifest workflow so another session cannot start a
   primary under an active read-only scan, then repeat all four manifests.
3. Review, qualify, and publish the separate v0.5.237 retrofit only from a
   clean worktree; do not mix it with this deployed v0.5.236 evidence.
